<?xml version="1.0" encoding="utf-8"?>

<resources>
    <attr name="titleTextStyleABS" format="reference" />
    <attr name="subtitleTextStyleABS" format="reference" />
    <attr name="backgroundABS" format="reference|color" />
    <attr name="backgroundSplitABS" format="reference|color" />
    <attr name="heightABS" format="dimension" />
    <attr name="dividerABS" format="reference" />

    <declare-styleable name="SherlockTheme">
        <!-- =================== -->
        <!-- Action bar styles   -->
        <!-- =================== -->
        <eat-comment />
        <!-- Default style for tabs within an action bar -->
        <attr name="actionBarTabStyleABS" format="reference" />
        <attr name="actionBarTabBarStyleABS" format="reference" />
        <attr name="actionBarTabTextStyleABS" format="reference" />
        <attr name="actionOverflowButtonStyleABS" format="reference" />
        <!-- Reference to a style for the Action Bar -->
        <attr name="actionBarStyleABS" format="reference" />
        <!-- Reference to a style for the split Action Bar. This style
             controls the split component that holds the menu/action
             buttons. actionBarStyle is still used for the primary
             bar. -->
        <attr name="actionBarSplitStyleABS" format="reference" />
        <!-- Reference to a theme that should be used to inflate widgets
             and layouts destined for the action bar. Most of the time
             this will be a reference to the current theme, but when
             the action bar has a significantly different contrast
             profile than the rest of the activity the difference
             can become important. If this is set to @null the current
             theme will be used.-->
        <attr name="actionBarWidgetThemeABS" format="reference" />
        <!-- Size of the Action Bar, including the contextual
             bar used to present Action Modes. -->
        <attr name="actionBarSizeABS" format="dimension" >
            <enum name="wrap_content" value="0" />
        </attr>
        <!-- Custom divider drawable to use for elements in the action bar. -->
        <attr name="actionBarDividerABS" format="reference" />
        <!-- Custom item state list drawable background for action bar items. -->
        <attr name="actionBarItemBackgroundABS" format="reference" />
        <!-- TextAppearance style that will be applied to text that
             appears within action menu items. -->
        <attr name="actionMenuTextAppearanceABS" format="reference" />
        <!-- Color for text that appears within action menu items. -->
        <attr name="actionMenuTextColorABS" format="color|reference" />

        <!-- =================== -->
        <!-- Action mode styles  -->
        <!-- =================== -->
        <eat-comment />
        <attr name="actionModeStyleABS" format="reference" />
        <attr name="actionModeCloseButtonStyleABS" format="reference" />
        <!-- Background drawable to use for action mode UI -->
        <attr name="actionModeBackgroundABS" format="reference" />
        <!-- Background drawable to use for action mode UI in the lower split bar -->
        <attr name="actionModeSplitBackgroundABS" format="reference" />
        <!-- Drawable to use for the close action mode button -->
        <attr name="actionModeCloseDrawableABS" format="reference" />
        <!-- Drawable to use for the Share action button in WebView selection action modes -->
        <attr name="actionModeShareDrawableABS" format="reference" />

        <!-- PopupWindow style to use for action modes when showing as a window overlay. -->
        <attr name="actionModePopupWindowStyleABS" format="reference" />

        <!-- ============= -->
        <!-- Button styles -->
        <!-- ============= -->
        <eat-comment />

        <!-- Small Button style. -->
        <attr name="buttonStyleSmallABS" format="reference" />

        <!-- Background drawable for standalone items that need focus/pressed states. -->
        <attr name="selectableItemBackgroundABS" format="reference" />



        <!-- This Drawable is overlaid over the foreground of the Window's content area, usually
             to place a shadow below the title.  -->
        <attr name="windowContentOverlayABS" format="reference" />

        <!-- Text color, typeface, size, and style for the text inside of a popup menu. -->
        <attr name="textAppearanceLargePopupMenuABS" format="reference" />

        <!-- Text color, typeface, size, and style for small text inside of a popup menu. -->
        <attr name="textAppearanceSmallPopupMenuABS" format="reference" />


        <!-- Text color, typeface, size, and style for "small" text. Defaults to secondary text color. -->
        <attr name="textAppearanceSmallABS" format="reference" />

        <attr name="textColorPrimaryABS" format="color" />
        <attr name="textColorPrimaryDisableOnlyABS" format="color" />
        <attr name="textColorPrimaryInverseABS" format="color" />

        <attr name="spinnerItemStyleABS" format="reference" />
        <attr name="spinnerDropDownItemStyleABS" format="reference" />

        <!-- ============================ -->
        <!-- SearchView styles and assets -->
        <!-- ============================ -->
        <eat-comment />
        <!-- SearchView AutoCompleteTextView style -->
        <attr name="searchAutoCompleteTextViewABS" format="reference" />
        <!-- SearchView dropdown background -->
        <attr name="searchDropdownBackgroundABS" format="reference" />
        <!-- SearchView close button icon -->
        <attr name="searchViewCloseIconABS" format="reference" />
        <!-- SearchView Go button icon -->
        <attr name="searchViewGoIconABS" format="reference" />
        <!-- SearchView Search icon -->
        <attr name="searchViewSearchIconABS" format="reference" />
        <!-- SearchView Voice button icon -->
        <attr name="searchViewVoiceIconABS" format="reference" />
        <!-- SearchView query refinement icon -->
        <attr name="searchViewEditQueryABS" format="reference" />
        <!-- SearchView query refinement icon background -->
        <attr name="searchViewEditQueryBackgroundABS" format="reference" />
        <!-- SearchView text field background for the left section -->
        <attr name="searchViewTextFieldABS" format="reference" />
        <!-- SearchView text field background for the right section -->
        <attr name="searchViewTextFieldRightABS" format="reference" />
        <!-- Text color for urls in search suggestions, used by things like global search and the browser. @hide -->
        <attr name="textColorSearchUrlABS" format="reference|color" />
        <!-- The list item height for search results. @hide -->
        <attr name="searchResultListItemHeightABS" format="dimension" />
        <!-- Text color, typeface, size, and style for system search result title. Defaults to primary inverse text color. -->
        <attr name="textAppearanceSearchResultTitleABS" format="reference" />
        <!-- Text color, typeface, size, and style for system search result subtitle. Defaults to primary inverse text color. -->
        <attr name="textAppearanceSearchResultSubtitleABS" format="reference" />


        <!-- =========== -->
        <!-- List styles -->
        <!-- =========== -->
        <eat-comment />

        <!-- A smaller, sleeker list item height. -->
        <attr name="listPreferredItemHeightSmallABS" format="dimension" />

        <!-- The preferred padding along the left edge of list items. -->
        <attr name="listPreferredItemPaddingLeftABS" format="dimension" />
        <!-- The preferred padding along the right edge of list items. -->
        <attr name="listPreferredItemPaddingRightABS" format="dimension" />

        <!-- The preferred TextAppearance for the primary text of small list items. -->
        <attr name="textAppearanceListItemSmallABS" format="reference" />


        <attr name="windowMinWidthMajor" format="dimension" />
        <attr name="windowMinWidthMinor" format="dimension" />



        <!-- Drawable to use for generic vertical dividers. -->
        <attr name="dividerVerticalABS" format="reference" />

        <attr name="actionDropDownStyleABS" format="reference" />
        <attr name="actionButtonStyleABS" format="reference" />
        <attr name="homeAsUpIndicatorABS" format="reference" />
        <attr name="dropDownListViewStyleABS" format="reference" />
        <attr name="popupMenuStyleABS" format="reference" />
        <attr name="dropdownListPreferredItemHeightABS" format="dimension" />
        <attr name="actionSpinnerItemStyle" format="reference" />
        <attr name="windowNoTitle" format="boolean"/>
        <attr name="windowActionBarABS" format="boolean"/>
        <attr name="windowActionBarOverlayABS" format="boolean"/>
        <attr name="windowActionModeOverlayABS" format="boolean"/>
        <attr name="windowSplitActionBar" format="boolean" />


        <attr name="listPopupWindowStyleABS" format="reference" />


        <!-- Default ActivityChooserView style. -->
        <attr name="activityChooserViewStyleABS" format="reference" />
        <!-- Drawable used as a background for activated items. -->
        <attr name="activatedBackgroundIndicator" format="reference" />

        <attr name="dropDownHintAppearance" format="reference" />
    </declare-styleable>


    <!-- Attributes used to style the Action Bar. -->
    <declare-styleable name="SherlockActionBar">
        <!-- The type of navigation to use. -->
        <attr name="navigationModeABS">
            <!-- Normal static title text -->
            <enum name="normal" value="0" />
            <!-- The action bar will use a selection list for navigation. -->
            <enum name="listMode" value="1" />
            <!-- The action bar will use a series of horizontal tabs for navigation. -->
            <enum name="tabMode" value="2" />
        </attr>
        <!-- Options affecting how the action bar is displayed. -->
        <attr name="displayOptionsABS">
            <flag name="useLogo" value="0x1" />
            <flag name="showHome" value="0x2" />
            <flag name="homeAsUp" value="0x4" />
            <flag name="showTitle" value="0x8" />
            <flag name="showCustom" value="0x10" />
            <flag name="disableHome" value="0x20" />
        </attr>
        <!-- Specifies title text used for navigationModeABS="normal" -->
        <attr name="titleABS" format="string" />
        <!-- Specifies subtitle text used for navigationModeABS="normal" -->
        <attr name="subtitleABS" format="string" />
        <!-- Specifies a style to use for title text. -->
        <attr name="titleTextStyleABS" />
        <!-- Specifies a style to use for subtitle text. -->
        <attr name="subtitleTextStyleABS" />
        <!-- Specifies the drawable used for the application icon. -->
        <attr name="iconABS" format="reference" />
        <!-- Specifies the drawable used for the application logo. -->
        <attr name="logoABS" format="reference" />
        <!-- Specifies the drawable used for item dividers. -->
        <attr name="dividerABS" />
        <!-- Specifies a background drawable for the action bar. -->
        <attr name="backgroundABS" />
        <!-- Specifies a background drawable for a second stacked row of the action bar. -->
        <attr name="backgroundStackedABS" format="reference|color" />
        <!-- Specifies a background drawable for the bottom component of a split action bar. -->
        <attr name="backgroundSplitABS" />
        <!-- Specifies a layout for custom navigation. Overrides navigationModeABS. -->
        <attr name="customNavigationLayoutABS" format="reference" />
        <!-- Specifies a fixed height. -->
        <attr name="heightABS" />
        <!-- Specifies a layout to use for the "home" section of the action bar. -->
        <attr name="homeLayoutABS" format="reference" />
        <!-- Specifies a style resource to use for an embedded progress bar. -->
        <attr name="progressBarStyleABS" format="reference" />
        <!-- Specifies a style resource to use for an indeterminate progress spinner. -->
        <attr name="indeterminateProgressStyleABS" format="reference" />
        <!-- Specifies the horizontal padding on either end for an embedded progress bar. -->
        <attr name="progressBarPaddingABS" format="dimension" />
        <!-- Specifies padding that should be applied to the left and right sides of
             system-provided items in the bar. -->
        <attr name="itemPaddingABS" format="dimension" />
    </declare-styleable>


    <declare-styleable name="SherlockActionMode">
        <!-- Specifies a style to use for title text. -->
        <attr name="titleTextStyleABS" />
        <!-- Specifies a style to use for subtitle text. -->
        <attr name="subtitleTextStyleABS" />
        <!-- Specifies a background for the action mode bar. -->
        <attr name="backgroundABS" />
        <!-- Specifies a background for the split action mode bar. -->
        <attr name="backgroundSplitABS" />
        <!-- Specifies a fixed height for the action mode bar. -->
        <attr name="heightABS" />
    </declare-styleable>

    <declare-styleable name="SherlockMenuView">
        <!-- Default appearance of menu item text. -->
        <attr name="itemTextAppearance" format="reference" />
        <!-- Default horizontal divider between rows of menu items. -->
        <attr name="horizontalDivider" format="reference" />
        <!-- Default vertical divider between menu items. -->
        <attr name="verticalDivider" format="reference" />
        <!-- Default background for the menu header. -->
        <attr name="headerBackground" format="color|reference" />
        <!-- Default background for each menu item. -->
        <attr name="itemBackground" format="color|reference" />
        <!-- Default animations for the menu. -->
        <attr name="windowAnimationStyle" format="reference" />
        <!-- Default disabled icon alpha for each menu item that shows an icon. -->
        <attr name="itemIconDisabledAlpha" format="float" />
        <!-- Whether space should be reserved in layout when an icon is missing. -->
        <attr name="preserveIconSpacingABS" format="boolean" />
    </declare-styleable>

    <declare-styleable name="SherlockActionMenuItemView">
        <attr name="android:minWidth" />
    </declare-styleable>

    <declare-styleable name="SherlockActivityChooserView">
        <!-- The maximal number of items initially shown in the activity list. -->
        <attr name="initialActivityCountABS" format="string" />
        <!-- The drawable to show in the button for expanding the activities overflow popup.
             <strong>Note:</strong> Clients would like to set this drawable
             as a clue about the action the chosen activity will perform. For
             example, if share activity is to be chosen the drawable should
             give a clue that sharing is to be performed.
         -->
        <attr name="expandActivityOverflowButtonDrawableABS" format="reference" />

        <attr name="android:background" />
    </declare-styleable>

    <!-- Base attributes that are available to all groups. -->
    <declare-styleable name="SherlockMenuGroup">

        <!-- The ID of the group. -->
        <attr name="android:id" />

        <!-- The category applied to all items within this group.
             (This will be or'ed with the orderInCategory attribute.) -->
        <attr name="android:menuCategory" />

        <!-- The order within the category applied to all items within this group.
             (This will be or'ed with the category attribute.) -->
        <attr name="android:orderInCategory" />

        <!-- Whether the items are capable of displaying a check mark. -->
        <attr name="android:checkableBehavior" />

        <!-- Whether the items are shown/visible. -->
        <attr name="android:visible" />

        <!-- Whether the items are enabled. -->
        <attr name="android:enabled" />

    </declare-styleable>

    <!-- Base attributes that are available to all Item objects. -->
    <declare-styleable name="SherlockMenuItem">

        <!-- The ID of the item. -->
        <attr name="android:id" />

        <!-- The category applied to the item.
             (This will be or'ed with the orderInCategory attribute.) -->
        <attr name="android:menuCategory" />

        <!-- The order within the category applied to the item.
             (This will be or'ed with the category attribute.) -->
        <attr name="android:orderInCategory" />

        <!-- The title associated with the item. -->
        <attr name="android:title" />

        <!-- The condensed title associated with the item.  This is used in situations where the
             normal title may be too long to be displayed. -->
        <attr name="android:titleCondensed" />

        <!-- The icon associated with this item.  This icon will not always be shown, so
             the title should be sufficient in describing this item. -->
        <attr name="android:icon" />

        <!-- The alphabetic shortcut key.  This is the shortcut when using a keyboard
             with alphabetic keys. -->
        <attr name="android:alphabeticShortcut" />

        <!-- The numeric shortcut key.  This is the shortcut when using a numeric (e.g., 12-key)
             keyboard. -->
        <attr name="android:numericShortcut" />

        <!-- Whether the item is capable of displaying a check mark. -->
        <attr name="android:checkable" />

        <!-- Whether the item is checked.  Note that you must first have enabled checking with
             the checkable attribute or else the check mark will not appear. -->
        <attr name="android:checked" />

        <!-- Whether the item is shown/visible. -->
        <attr name="android:visible" />

        <!-- Whether the item is enabled. -->
        <attr name="android:enabled" />

        <!-- Name of a method on the Context used to inflate the menu that will be
             called when the item is clicked. -->
        <attr name="android:onClick" />

        <!-- How this item should display in the Action Bar, if present. -->
        <attr name="android:showAsAction" />

        <!-- An optional layout to be used as an action view.
             See {@link android.view.MenuItem#setActionView(android.view.View)}
             for more info. -->
        <attr name="android:actionLayout" />

        <!-- The name of an optional View class to instantiate and use as an
             action view. See {@link android.view.MenuItem#setActionView(android.view.View)}
             for more info. -->
        <attr name="android:actionViewClass" />

        <!-- The name of an optional ActionProvider class to instantiate an action view
             and perform operations such as default action for that menu item.
             See {@link android.view.MenuItem#setActionProvider(android.view.ActionProvider)}
             for more info. -->
        <attr name="android:actionProviderClass" />

    </declare-styleable>

    <declare-styleable name="SherlockSpinner">
        <!-- The prompt to display when the spinner's dialog is shown. -->
        <attr name="android:prompt" />
        <!-- List selector to use for spinnerMode="dropdown" display. -->
        <attr name="android:dropDownSelector" />
        <!-- Background drawable to use for the dropdown in spinnerMode="dropdown". -->
        <attr name="android:popupBackground" />
        <!-- Vertical offset from the spinner widget for positioning the dropdown in
             spinnerMode="dropdown". -->
        <attr name="android:dropDownVerticalOffset" />
        <!-- Horizontal offset from the spinner widget for positioning the dropdown
             in spinnerMode="dropdown". -->
        <attr name="android:dropDownHorizontalOffset" />
        <!-- Width of the dropdown in spinnerMode="dropdown". -->
        <attr name="android:dropDownWidth" />
        <!-- Reference to a layout to use for displaying a prompt in the dropdown for
             spinnerMode="dropdown". This layout must contain a TextView with the id
             @android:id/text1 to be populated with the prompt text. -->
        <attr name="android:popupPromptView" />
        <!-- Gravity setting for positioning the currently selected item. -->
        <attr name="android:gravity" />
    </declare-styleable>

    <declare-styleable name="SherlockSearchView">
        <!-- The default state of the SearchView. If true, it will be iconified when not in
             use and expanded when clicked. -->
        <attr name="iconifiedByDefaultABS" format="boolean"/>
        <!-- An optional maximum width of the SearchView. -->
        <attr name="android:maxWidth" />
        <!-- An optional query hint string to be displayed in the empty query field. -->
        <attr name="queryHintABS" format="string" />
        <!-- The IME options to set on the query text field. -->
        <attr name="android:imeOptions" />
        <!-- The input type to set on the query text field. -->
        <attr name="android:inputType" />
    </declare-styleable>

    <declare-styleable name="SherlockView">
        <attr name="android:focusable"/>
    </declare-styleable>
</resources>
